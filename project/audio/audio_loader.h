#pragma once
#ifndef AUDIO_LOADER_H
#define AUDIO_LOADER_H

/// Completely de-facto independent of the rest of the audio system.
/// Merely feeds track names from `music.rsdt`.
/// - If the loading failed, undefs `USING_AUDIO` (hence the inclusion of `audio.h`)

#endif /// AUDIO_LOADER_H